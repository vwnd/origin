# Speckle personal access token.
# Create one at https://app.speckle.systems -> Settings -> Developer Settings -> Access Tokens.
# Listing projects needs both scopes below:
#   profile:read  (the API's activeUser query, reported by the server as user.profile.read)
#   streams:read  (read the projects themselves)
SPECKLE_TOKEN=
